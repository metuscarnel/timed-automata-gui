J'étends mon éditeur d'automates en PySide6 pour y inclure la gestion de variables de données. Je dois ajouter un bouton/icone (Datas) dans ma barre d'outils existante pour ouvrir une nouvelle fenêtre d'édition dédiée.

Travail demandé :

Créer une nouvelle classe DataEditorDialog(QDialog) :

Définir un layout principal (ex: QVBoxLayout).

Ajouter un QTabWidget avec 3 onglets (ex: "Variables", "Opérations", "Paramètres").

Ajouter des layouts vides basiques dans chaque onglet pour que je puisse les remplir plus tard.

Ajouter une QDialogButtonBox (Ok / Annuler) en bas pour fermer la fenêtre.

Mettre à jour la Vue principale (MainWindow) :

Montrer comment ajouter un nouveau bouton (Action) "Éditeur de Données" dans ma QToolBar existante.

Connecter ce bouton à une méthode open_data_editor(self) qui instancie et affiche DataEditorDialog via .exec().

Format de sortie : Génère uniquement le code Python clair et commenté pour la classe DataEditorDialog et l'extrait de code pour la barre d'outils.